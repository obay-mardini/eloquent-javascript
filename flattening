<!DOCTYPE html>
<html>
	<head>
	</head>
	<body>
	  <script>
		  /*
      Use the reduce method in combination with the concat method
			to “flatten” an array of arrays into a single array that has all the elements of the input arrays.
	    */
			
			var arrays = [[1,2,3], [4, 5, 5], [7, 8, 9]];
			  var x = arrays.reduce(function(start, current) {
				  return start.concat(current);
				});
		</script>
	</body>
</html>